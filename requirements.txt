# ── Mac workstation (run inside:  conda activate yolo) ─────────
# The conda env `yolo` already has all of these.
Pillow>=9.0            # camera frame decode/draw in the GUI
ultralytics>=8.0       # runs best.pt (YOLO) fully offline
# note: Intel Macs need torch<=2.2.x and numpy<2 (conda env handles this)

# Optional (GUI runs without these):
# tkmacosx             # nicer flat buttons on macOS
# SpeechRecognition    # voice command override (voice_engine.py)

# ── Robot side (robot/ folder → ~/delivery/ on the Jetson) ─────
# NO pip installs needed: uses rospy, actionlib, hiwonder_servo_msgs
# from the JetAuto image (source ~/jetauto_ws/devel/setup.bash).
# Optional on-robot fallback detector only:
# inference            # Roboflow local inference
# opencv-python
